<!DOCTYPE html>
<html lang="vi">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
        <link rel="stylesheet" href="css/user/form.css">
        <link rel="stylesheet" href="css/boostrap.css">
        <title>Quên Mật Khẩu</title>
    </head>

    <body>
        <div class="container d-flex justify-content-center align-items-center auth-container" style="min-height: 100vh;">
            <div class="card card-custom" style="max-width: 500px; width: 100%;">

                <!-- Title -->
                <div class="text-center mb-4">
                    <h3 class="mt-3">Quên mật khẩu</h3>
                </div>

                <!-- Form Nhập Email + Số điện thoại -->
                <form id="forgotForm" action="/forgot-password" method="post">
                    <p class="text-center">Nhập email để xác thực và nhận mã OTP.</p>
                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fa fa-envelope"></i></span>
                            <input type="email" class="form-control" placeholder="Nhập email của bạn" required name="email">
                        </div>
                    </div>
                    <button type="submit" class="btn btn-success w-100 rounded-pill py-2">
                        Gửi mã OTP
                    </button>
                </form>

                <!-- Form Nhập OTP - Ẩn ban đầu -->
                <div id="otpSection" style="display: none;">
                    <hr class="my-4">
                    <div class="d-flex align-items-center mb-3">
                        <button type="button" id="backToForgotForm" class="btn btn-link p-0 me-2 text-decoration-none">
                            <i class="fa fa-arrow-left fa-lg"></i>
                        </button>
                        <h4 class="m-0">Xác thực OTP</h4>
                    </div>

                    <p class="text-center mb-4">
                        Chúng tôi đã gửi mã OTP đến email của bạn. <br>
                        Vui lòng kiểm tra và nhập mã bên dưới.
                    </p>

                    <form id="otpForm" action="/forgot-password/verify-otp" method="post">
                        <div class="mb-3">
                            <label class="form-label">Mã OTP</label>
                            <input type="text" class="form-control" placeholder="Nhập mã OTP" required name="otp">
                        </div>
                        <button type="submit" class="btn btn-primary w-100 rounded-pill py-2">
                            Xác thực
                        </button>
                    </form>
                </div>

                <div class="text-center mt-3">
                    <h6>
                        <a href="/login" class="text-decoration-none">
                            <i class="fa fa-arrow-left me-1"></i> Quay lại đăng nhập
                        </a>
                    </h6>
                </div>

            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            // Xử lý chuyển form
            document.getElementById("forgotForm").addEventListener("submit", function(e) {
                e.preventDefault();

                // Thực hiện gửi dữ liệu lên backend nếu muốn
                console.log("Gửi email + phone để nhận OTP...");

                // Ẩn form nhập email + phone
                document.getElementById("forgotForm").style.display = "none";

                // Hiện form nhập OTP
                document.getElementById("otpSection").style.display = "block";
            });

            document.getElementById("otpForm").addEventListener("submit", function(e) {
                e.preventDefault();

                let otpValue = this.otp.value;
                console.log("Người dùng nhập OTP:", otpValue);

                // Thực hiện gọi API xác thực OTP ở backend nếu muốn
                alert("Xác thực OTP thành công! Chuyển sang đặt lại mật khẩu...");

                // Ví dụ: Redirect sang trang reset-password
                // window.location.href = "/reset-password";
            });

            document.getElementById("backToForgotForm").addEventListener("click", function() {
                document.getElementById("otpSection").style.display = "none";
                document.getElementById("forgotForm").style.display = "block";
            });
        </script>
    </body>
</html>
