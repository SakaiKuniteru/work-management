<div class="container mt-4">

  <!-- Nút quay lại -->
  <div class="mb-3">
    <a href="/projects-list" class="btn btn-outline-secondary back-link">
      <i class="bi bi-arrow-left"></i> Quay lại
    </a>
  </div>

  <!-- Tiêu đề -->
  <div class="mb-4">
    <h2 id="projectTitle">Dự án A</h2>
    <span class="badge bg-warning text-dark">Đang triển khai</span>
    <span class="badge bg-danger">Cao</span>
  </div>

  <!-- Thông tin chi tiết -->
  <div class="row mb-4">
    <div class="col-md-6">
      <p><strong>Ngày bắt đầu:</strong> <span id="projectStartDate">01/06/2025</span></p>
      <p><strong>Hạn hoàn thành:</strong> <span id="projectEndDate">30/08/2025</span></p>
      <p><strong>Người quản lý:</strong> <span id="projectManager">Nguyễn Văn A</span></p>
      <p><strong>Email quản lý:</strong> <span id="managerEmail">nva@example.com</span></p>
      <p><strong>Điện thoại quản lý:</strong> <span id="managerPhone">0123456789</span></p>
    </div>
    <div class="col-md-6">
      <p><strong>Người tham gia:</strong> <span id="projectMembers">Trần Thị B, Lê Văn C</span></p>
      <p><strong>Email người tham gia:</strong> <span id="membersEmail">ttb@example.com, lvc@example.com</span></p>
      <p><strong>Điện thoại người tham gia:</strong> <span id="membersPhone">0987654321, 0912345678</span></p>
      <p><strong>Số lượng:</strong> <span id="projectCapacity">0/2</span></p>
    </div>
  </div>

  <div class="mb-4">
    <h5>Mô tả dự án</h5>
    <p id="projectDescription">
      Dự án A nhằm phát triển hệ thống quản lý dữ liệu cho khách hàng, bao gồm xây dựng backend, frontend và các báo cáo phân tích. Thời gian triển khai dự kiến 3 tháng.
    </p>
  </div>

  <!-- Thêm ghi chú & upload file -->
  <div class="mb-4">
    <h5>Thêm ghi chú / gửi file</h5>
    <form id="noteForm">
      <div class="mb-3">
        <textarea class="form-control" id="projectNote" rows="4" placeholder="Nhập ghi chú hoặc ý kiến của bạn..."></textarea>
      </div>
      <div class="mb-3">
        <label for="fileUpload" class="form-label">Tải tệp đính kèm</label>
        <input class="form-control" type="file" id="fileUpload" name="fileUpload">
      </div>
      <button type="submit" class="btn btn-primary">Gửi</button>
    </form>
  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
  document.getElementById("noteForm").addEventListener("submit", (e) => {
    e.preventDefault();

    const note = document.getElementById("projectNote").value;
    const file = document.getElementById("fileUpload").files[0];

    console.log("Nội dung ghi chú:", note);
    if (file) {
      console.log("Tên file:", file.name);
    }

    alert("Đã gửi ghi chú và/hoặc tệp đính kèm!");
    document.getElementById("noteForm").reset();
  });
</script>